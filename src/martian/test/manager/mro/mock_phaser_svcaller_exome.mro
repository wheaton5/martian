@include "_mock_stages.mro"

pipeline MOCK_PHASER_SVCALLER_EXOME(
    in  map[]      sample_def,
    in  map[]      downsample,
)
{
    call PROCESS_FASTQS(
        sample_def = self.sample_def,
        downsample = self.downsample,
    )

    return (
    )
}