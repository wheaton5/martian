@include "_mock_stages.mro"

pipeline MOCK_SC_VDJ_PD(
    in  map[]      sample_def,
)
{
    call PROCESS_FASTQS(
        sample_def = self.sample_def,
        downsample = null,
    )

    return (
    )
}